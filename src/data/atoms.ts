import type { CategoryId } from './categories';

export interface Atom {
  id: string;
  name: string;
  cat: CategoryId;
  maturity: number;
  desc: string;
  benefits: string[];
  hurts: string[];
  openQs: string[];
  refs: string[];
}

export const ATOMS: Atom[] = [
  // ── Category A: Erasure coding ─────────────────────────────────────
  {
    id: 'A1',
    name: '1D row extension (RS)',
    cat: 'A',
    maturity: 5,
    desc: 'Each blob is extended horizontally to 2x width using RS coding. The extended blob becomes a row in the data matrix. Columns (vertical slices across all rows) become the sampling unit. The extension can be performed by the blob transaction sender, off the critical path.',
    benefits: ['P2', 'P4', 'P10', 'P11'],
    hurts: [],
    openQs: [
      'With 1D extension only, reconstruction requires collecting >=50% of all columns, which means only supernodes or semi-supernodes can reconstruct. Acceptable long-term?',
      'At very high blob counts, does the column size (growing linearly) create practical gossip message size issues?',
    ],
    refs: ['EIP-4844', 'EIP-7594: PeerDAS'],
  },
  {
    id: 'A2',
    name: '2D extension (RS)',
    cat: 'A',
    maturity: 2,
    desc: 'Data is extended both horizontally (row-wise) and vertically (column-wise) using a bivariate polynomial. Creates a full matrix where any individual row or column can be independently repaired from >=50% of its cells. The second dimension must be computed at block construction time (on the critical path), unless combined with EL blob encoding.',
    benefits: ['P5', 'P6', 'P3', 'P1'],
    hurts: [],
    openQs: [
      'Vertical extension computation is on the critical path for the builder. How much does A4 (EL blob encoding) offset this?',
      'What is the concrete latency overhead of computing the second dimension at block time for 128+ blobs?',
      "Benedikt+Francesco analysis: is 2D worth complexity if 1D with thinner columns achieves similar security?",
    ],
    refs: ['FullDAS (cskiraly, May 2024)', 'FullDASv2 (cskiraly, May 2025)', 'Revisiting secure DAS (Benedikt+Francesco, Jul 2025)'],
  },
  {
    id: 'A3',
    name: 'RLNC (Random Linear Network Coding)',
    cat: 'A',
    maturity: 1,
    desc: 'Replace fixed RS cells with random linear combinations of the data. Any K linearly independent combinations suffice to decode the full data. Sub-variants: A3a I-RLC (interactive), A3b NI-RLC (non-interactive), A3c R-RLC (restricted), A3d column-restricted RLC.',
    benefits: ['P3', 'P13', 'P7'],
    hurts: ['P5', 'P6', 'P10'],
    openQs: [
      'How to make RLNC verifiable without per-combination KZG proofs?',
      'Gameable by adversary controlling coefficient selection?',
      'Compatible with pipelined dissemination?',
      'Design no. 3 (individualized samples without P2P redistribution) effectively creates a client-server model. Is there a viable design that preserves P2P redistribution while getting RLNC\'s probabilistic benefits?',
    ],
    refs: ['Alternative DAS concept based on RLNC (Kolotov et al., Jun 2025)', 'FullDASv2 (cskiraly, May 2025)'],
  },
  {
    id: 'A4',
    name: 'EL blob encoding',
    cat: 'A',
    maturity: 3,
    desc: 'Row-wise RS erasure coding is performed when the blob transaction is submitted to the mempool, rather than at block construction time. Blobs arrive at nodes already extended. Reduces computation for the builder and for nodes using getBlobs.',
    benefits: ['P4', 'P11', 'P15'],
    hurts: [],
    openQs: [
      "Who bears the cost of KZG proof computation for the extended cells? The blob tx sender? This adds cost to L2 sequencers.",
      "Does pre-encoding in the mempool change the DOS surface for the EL gossip layer?",
    ],
    refs: ['FullDASv2 (cskiraly, May 2025)', 'EIP-7594'],
  },
  {
    id: 'A5',
    name: 'Sender-side cell proofs',
    cat: 'A',
    maturity: 5,
    desc: 'Blob transaction senders compute cell-level KZG proofs and include them in the EIP-4844 transaction wrapper. Nodes validate that commitments match versioned hashes and verify that cell proofs match the blob\'s cells (including extension cells). Batch verification (via verify_cell_kzg_proof_batch) is explicitly specified.',
    benefits: ['P15', 'P4'],
    hurts: [],
    openQs: [
      'Pushing DAS cryptography into EL and mempool rules tightens coupling between the two layers. Alternative paths may be needed if the proof format evolves.',
      'What is the practical batch size before diminishing returns? How does batch verification interact with streaming cell arrival?',
    ],
    refs: ['EIP-7594', 'A universal verification equation for DAS (Kadianakis, Dietrichs, Feist, Aug 2022)'],
  },

  // ── Category B: Messaging ──────────────────────────────────────────
  {
    id: 'B1',
    name: 'Column-level messaging',
    cat: 'B',
    maturity: 5,
    desc: 'The unit of P2P gossip is an entire column (a vertical slice across all blobs in a block). Each column message contains one cell per blob. Used in current PeerDAS.',
    benefits: ['P10', 'P14'],
    hurts: ['P5', 'P12'],
    openQs: [
      'Column size grows linearly with blob count. At 128 blobs, a single column message is ~64 KiB. At 256 blobs, ~128 KiB. Practical GossipSub message size limits?',
      "With column-level messages, if a node's EL is missing even one blob, it cannot assemble a full column from getBlobs. Cliff effect at scale.",
    ],
    refs: ['EIP-7594', 'PeerDAS documentation (fradamt + b-wagn, Aug 2024)'],
  },
  {
    id: 'B2',
    name: 'Cell-level messaging',
    cat: 'B',
    maturity: 2,
    desc: 'The unit of P2P gossip is a single cell (the intersection of one row and one column in the data matrix). Any custodying node can send or receive individual cells. Prerequisite for local reconstruction without supernodes.',
    benefits: ['P5', 'P6', 'P12', 'P3'],
    hurts: ['P10'],
    openQs: [
      'Per-message overhead is the key concern. At 256 blobs with 128 columns, the matrix has 32,768 cells per slot. Is the per-message processing cost tractable?',
      "How does cell-level gossip interact with GossipSub's IHAVE/IWANT protocol? Signaling overhead could dominate bandwidth.",
      "Anti-DOS: how do you validate a single cell before forwarding? You need block header/KZG commitments first.",
    ],
    refs: ['FullDAS (cskiraly, May 2024)', 'FullDASv2 (cskiraly, May 2025)', 'SubnetDAS discussion (fradamt, Oct 2023)'],
  },
  {
    id: 'B3',
    name: 'GossipSub partial messages',
    cat: 'B',
    maturity: 3,
    desc: 'A backwards-compatible GossipSub upgrade that allows nodes to disseminate partial messages (cells) within existing column topics, without requiring a hard fork or new topic structure. Uses a group ID (block root) and bitmap (indexing cells) so peers can advertise and request just the missing pieces. A devnet PoC reportedly reduced data sent for data columns by ~10x in a two-peer experiment.',
    benefits: ['P14', 'P3', 'P12', 'P9'],
    hurts: [],
    openQs: [
      'How do non-upgraded nodes handle partial messages they receive?',
      'Scaling to real meshes with realistic RTTs, tuning eager-push probabilities, and integrating with IDONTWANT/mesh behaviour are still open engineering problems.',
      'Does the extension compose cleanly with PPPT (F2) and batch publishing (F1)?',
    ],
    refs: ["GossipSub partial messages extension (MarcoPolo, Sep 2025)"],
  },
  {
    id: 'B4',
    name: 'Mixed column+cell messaging',
    cat: 'B',
    maturity: 1,
    desc: 'Use column-level messages as the primary transport but allow cell-level messages as a secondary path for repair and cross-forwarding. A compromise that manages per-message overhead while retaining cell-level repair benefits.',
    benefits: ['P10', 'P5'],
    hurts: [],
    openQs: [
      'What triggers the switch from column to cell messaging? Automatic (e.g. after timeout) or always parallel?',
      'How do you avoid double-counting when the same data arrives via both paths?',
    ],
    refs: ['FullDASv2 (cskiraly, May 2025)'],
  },

  // ── Category C: Subnet topology ────────────────────────────────────
  {
    id: 'C1',
    name: 'Column subnets',
    cat: 'C',
    maturity: 5,
    desc: "One gossip subnet per column (or per custody group of columns). Nodes subscribe to a deterministic set based on their node ID. Used for data distribution and sampling in all DAS proposals. Custody is expressed in \"custody groups\" and advertised via ENR (cgc).",
    benefits: ['P9', 'P10'],
    hurts: ['P8'],
    openQs: [
      'The number of custody groups and their mapping to subnets affects subnet density. Too many subnets + too few nodes per subnet = unreliable gossip.',
      'Deterministic custody improves coordination but aggravates query-linkability risks.',
    ],
    refs: ['EIP-7594', 'PeerDAS original post (Sep 2023)'],
  },
  {
    id: 'C2',
    name: 'Row subnets',
    cat: 'C',
    maturity: 1,
    desc: 'One gossip subnet per row (i.e. per blob position in the block). Used for blob distribution and for cross-seeding recovered cells. In SubnetDAS, only validators join row subnets.',
    benefits: ['P6', 'P5'],
    hurts: ['P10', 'P2'],
    openQs: [
      'Row indices correspond to blob positions in a block. Not known until the block is proposed. How do nodes pre-join row subnets?',
      'At 256 blobs, you need 256 row subnets. Combined with 128 column subnets, that\'s 384 total subnets. Too many for practical peer management?',
    ],
    refs: ['SubnetDAS (fradamt + Ansgar, Oct 2023)', 'FullDAS (cskiraly, May 2024)'],
  },
  {
    id: 'C3',
    name: 'Stable + rotating subnet assignment',
    cat: 'C',
    maturity: 4,
    desc: 'Nodes join k stable column subnets (for density and data retrievability) plus k rotating ones (for sampling freshness). Proposed in SubnetDAS to partially mitigate query linkability.',
    benefits: ['P8', 'P9'],
    hurts: ['P10'],
    openQs: [
      'Rotation period tradeoff: too fast = churn overhead, too slow = predictable targets.',
      'Even with rotation, the act of joining/leaving subnets (peer grafting) is visible to an adversary.',
    ],
    refs: ['EIP-7594', 'SubnetDAS (fradamt + Ansgar, Oct 2023)'],
  },
  {
    id: 'C4',
    name: 'Row/column ID-based peer discovery',
    cat: 'C',
    maturity: 2,
    desc: 'Nodes advertise their custody rows and columns in their ENR. Peers can be discovered based on shared custody interests, eliminating connection delay when a node needs to find peers for a specific dimension.',
    benefits: ['P4', 'P7'],
    hurts: ['P8'],
    openQs: [
      'ENR size is limited. How many custody IDs can be advertised before hitting practical limits?',
      'Does advertising custody assignments in ENR worsen query linkability?',
    ],
    refs: ['EIP-7594', 'FullDAS (cskiraly, May 2024)'],
  },

  // ── Category D: Overhead reduction ────────────────────────────────
  {
    id: 'D1',
    name: 'Structured message IDs',
    cat: 'D',
    maturity: 1,
    desc: 'Derive GossipSub message IDs from (row, column) coordinates rather than hashing the full payload. Reduces message ID size and computation in IHAVE/IWANT exchanges.',
    benefits: ['P2', 'P4'],
    hurts: [],
    openQs: [
      'Requires GossipSub-level changes. Adoption path?',
    ],
    refs: ['FullDAS (cskiraly, May 2024)'],
  },
  {
    id: 'D2',
    name: 'Bitmap IHAVE/IWANT',
    cat: 'D',
    maturity: 1,
    desc: 'Instead of listing individual cell message IDs, use bitmaps to represent which cells a node has or wants in a given row/column. Compresses signaling overhead dramatically at high cell counts.',
    benefits: ['P2', 'P1'],
    hurts: [],
    openQs: [
      'Bitmap size at 256 blobs x 128 columns = 4 KiB. Acceptable overhead?',
      'Interaction with GossipSub heartbeat interval?',
    ],
    refs: ['FullDAS (cskiraly, May 2024)'],
  },
  {
    id: 'D3',
    name: 'Batch KZG verification',
    cat: 'D',
    maturity: 4,
    desc: 'Verify multiple KZG cell proofs at once using amortized verification equations, rather than checking each cell proof independently. See also A5 (sender-side proofs), which ensures the proofs are available to verify.',
    benefits: ['P4', 'P1', 'P15'],
    hurts: [],
    openQs: [
      'What is the practical batch size before diminishing returns? How does this interact with streaming cell arrival?',
    ],
    refs: ['A universal verification equation for DAS (Kadianakis, Dietrichs, Feist, Aug 2022)', 'EIP-7594'],
  },

  // ── Category E: Reconstruction / Engine API ───────────────────────
  {
    id: 'E1',
    name: 'Supernode full-matrix reconstruction',
    cat: 'E',
    maturity: 5,
    desc: "A node subscribes to all 128 column subnets, collects all columns, and can reconstruct any missing rows via RS decoding. Spec-defined behavior includes cross-seeding reconstructed columns back to the network.",
    benefits: ['P5', 'P7'],
    hurts: ['P3'],
    openQs: [
      'Supernode count on mainnet is uncertain. If too few supernodes exist, the network becomes dependent on a small set of well-resourced nodes.',
      "Sigma Prime's tests showed supernodes consume significantly higher CL bandwidth. Minimum viable supernode count?",
    ],
    refs: ['Sigma Prime blog: PeerDAS and distributed blob building (Sep 2024)', 'EIP-7594'],
  },
  {
    id: 'E1b',
    name: 'Desynchronized reconstruction',
    cat: 'E',
    maturity: 3,
    desc: 'Supernodes add a random delay before reconstruction to avoid synchronized bandwidth spikes. Once reconstruction yields a column, the node treats it as if it arrived from the network. This is normative spec guidance, not a separate mechanism, but it is a distinct design decision that affects timing and load distribution.',
    benefits: ['P4', 'P7'],
    hurts: [],
    openQs: [
      '"When to reconstruct" remains an explicit timing and anti-DoS question in the spec notes. Sigma Prime flagged a race condition where reconstruction and proof computation compete for CPU simultaneously.',
    ],
    refs: ['Fulu das-core "Reconstruction and cross-seeding" section'],
  },
  {
    id: 'E2',
    name: 'Semi-supernode reconstruction',
    cat: 'E',
    maturity: 4,
    desc: 'Subscribe to exactly enough columns (64 out of 128) to hit the 50% reconstruction threshold. Minimum viable setup for serving the beacon blob API.',
    benefits: ['P2', 'P5'],
    hurts: [],
    openQs: [
      'At exactly 50%, any single missing column makes reconstruction fail. How sensitive is this to network conditions?',
    ],
    refs: ['Prysm docs (--semi-supernode flag)', 'EIP-7594'],
  },
  {
    id: 'E2b',
    name: 'getBlobsV2 all-or-nothing semantics',
    cat: 'E',
    maturity: 5,
    desc: 'The Osaka Engine API defines engine_getBlobsV2 as returning null if any requested blob is missing or unavailable. Partial availability is not surfaced. This is a named constraint rather than a feature — it directly limits the utility of other reconstruction approaches and is the primary motivation for getBlobsV3.',
    benefits: ['P10'],
    hurts: ['P12', 'P7'],
    openQs: [
      'This is directly identified as an inefficiency: "if the EL misses just one blob, the column can\'t be created, and getBlobs is rendered useless." The primary motivation for getBlobsV3 (E8).',
    ],
    refs: ['FullDASv2 (cskiraly, May 2025)', 'Engine API spec'],
  },
  {
    id: 'E3',
    name: 'Per-row local reconstruction',
    cat: 'E',
    maturity: 2,
    desc: 'A node with >50% of cells in a single row decodes the missing cells using RS. Does not require global data. Any custodying node can do it.',
    benefits: ['P3', 'P7', 'P5'],
    hurts: [],
    openQs: [
      'With 1D extension only, per-row local reconstruction is effectively supernode-level. Practically requires 2D extension.',
      'With cell-level messaging and many nodes contributing cells, can enough accumulate in row subnets for reconstruction even with 1D?',
    ],
    refs: ['FullDAS (cskiraly, May 2024)', 'SubnetDAS discussion (fradamt, Oct 2023)'],
  },
  {
    id: 'E4',
    name: 'Per-column local reconstruction',
    cat: 'E',
    maturity: 2,
    desc: 'Same as E3 but along the column dimension. A node with >50% of cells in a single column decodes the rest.',
    benefits: ['P3', 'P5', 'P7'],
    hurts: [],
    openQs: [
      'Column reconstruction is useful for repair but does not help nodes that need data from other columns.',
    ],
    refs: ['FullDAS (cskiraly, May 2024)'],
  },
  {
    id: 'E5',
    name: 'Cross-forwarding / cross-seeding',
    cat: 'E',
    maturity: 2,
    desc: 'After recovering a cell at position (row_i, col_j) via row repair, the node pushes that cell to col_j\'s subnet (and vice versa). Creates an availability amplification feedback loop between dimensions.',
    benefits: ['P6', 'P3', 'P1'],
    hurts: ['P10'],
    openQs: [
      'How fast does the cross-forwarding feedback loop converge? Is there a risk of oscillation or flooding?',
      'RLNC (A3) breaks cross-forwarding because coded pieces from one dimension do not help the other. Dealbreaker for RLNC adoption?',
    ],
    refs: ['FullDAS (cskiraly, May 2024)', 'SubnetDAS discussion (fradamt, Oct 2023)'],
  },
  {
    id: 'E6',
    name: 'getBlobs-assisted column assembly',
    cat: 'E',
    maturity: 5,
    desc: "CL fetches complete blobs (rows) from the EL mempool via the engine API (engine_getBlobsV1/V2). If all blobs are present, full columns can be assembled without waiting for CL gossip. Primary mechanism for PeerDAS dissemination.",
    benefits: ['P4', 'P11'],
    hurts: [],
    openQs: [
      "Only useful when EL has all blobs. Degrades with private orderflow and sharded/partial mempools.",
      "Cliff effect: with column-level messaging, missing even one blob makes getBlobs useless for that column.",
    ],
    refs: ['Sigma Prime blog (Sep 2024)', 'Is data available in the EL mempool? (cskiraly)'],
  },
  {
    id: 'E7',
    name: 'getBlobs cell injection',
    cat: 'E',
    maturity: 2,
    desc: 'Even if the EL has only one blob, the CL can extract individual cells from that blob and push them into column subnets. Does not require all blobs to be present. Works with partial/sharded mempools.',
    benefits: ['P12', 'P3'],
    hurts: ['P10'],
    openQs: [
      'If a node is not subscribed to a column subnet, should it push the cell or just gossip its availability?',
      'Does injecting EL-sourced cells into CL gossip create validation issues?',
    ],
    refs: ['FullDASv2 (cskiraly, May 2025)'],
  },
  {
    id: 'E8',
    name: 'getBlobsV3 (partial responses)',
    cat: 'E',
    maturity: 4,
    desc: 'engine_getBlobsV3 keeps the same request format as V2 but returns an array of equal length where missing blobs are null at those positions. The CL learns which blobs are locally available and can exchange only missing parts with peers.',
    benefits: ['P12', 'P7', 'P9'],
    hurts: [],
    openQs: [
      'How do CL clients negotiate and opportunistically use V3 without breaking backwards compatibility with V2?',
      'How quickly do EL clients prune blobs, and does this affect the null patterns in ways that confuse the CL?',
    ],
    refs: ['FullDASv2 (cskiraly, May 2025)', 'GossipSub partial messages extension (Sep 2025)', 'Engine API spec (execution-apis PR #719)'],
  },
  {
    id: 'E9',
    name: 'RLNC-based full reconstruction',
    cat: 'E',
    maturity: 2,
    desc: 'Collect K linearly independent coded pieces from anywhere in the matrix. Decode all at once. No per-row/per-column partial repair.',
    benefits: ['P13', 'P3'],
    hurts: ['P5', 'P6'],
    openQs: [
      '"Any K pieces" sounds good in theory, but how does a node accumulate K pieces without per-dimension gossip structure? May degenerate into a client-server model.',
    ],
    refs: ['Alternative DAS concept based on RLNC (Kolotov et al., Jun 2025)'],
  },
  {
    id: 'E10',
    name: 'getBlobsV4 (cell-level Engine API)',
    cat: 'E',
    maturity: 2,
    desc: "engine_getBlobsV4 extends the Engine API to cell-level granularity. Accepts an indices_bitarray parameter (uint128) specifying exactly which cell indices to retrieve. Returns per-blob cell arrays with null entries for missing cells, plus corresponding KZG proofs. Introduced alongside engine_blobCustodyUpdatedV1, which lets the CL inform the EL of its current custody column set.",
    benefits: ['P2', 'P9', 'P12'],
    hurts: [],
    openQs: [
      'How does V4 interact with V3? Are they alternatives or do they coexist?',
      'The response includes KZG proofs per cell. With 128 cells per blob and 128+ blobs, is proof batching across the Engine API boundary practical?',
      '500ms timeout: at high blob counts, is this sufficient?',
    ],
    refs: ['EIP-8070: Sparse Blobpool (Oct 2025)'],
  },

  // ── Category F: Publisher optimization ────────────────────────────
  {
    id: 'F1',
    name: 'Batch publishing',
    cat: 'F',
    maturity: 2,
    desc: 'Supernode publishes cells to multiple column subnets in parallel batches rather than one-at-a-time. Reduces source-side overhead. cskiraly\'s measurements show lower latency than standard GossipSub for DAS.',
    benefits: ['P4', 'P11'],
    hurts: [],
    openQs: [
      'Optimal batch size and scheduling strategy as a function of network size and blob count?',
      "Interaction with GossipSub's eager/lazy push mechanisms?",
    ],
    refs: ['Improving DAS performance with GossipSub batch publishing (cskiraly, Feb 2025)'],
  },
  {
    id: 'F2',
    name: 'Push-pull phase transition (PPPT)',
    cat: 'F',
    maturity: 2,
    desc: 'Start with push-based gossip for initial speed, then transition to pull-based (IHAVE/IWANT) once enough data is in the network. Reduces duplicate bandwidth by 2x or more without significantly increasing latency.',
    benefits: ['P2', 'P1'],
    hurts: [],
    openQs: [
      'When to trigger the phase transition? Too early = slow propagation; too late = wasted duplicates.',
      'How does this interact with heterogeneous node bandwidth?',
    ],
    refs: ['PPPT (cskiraly, Apr 2025)', 'Doubling the blob count with GossipSub v2.0'],
  },
  {
    id: 'F3',
    name: 'Gradual publication (supernodes)',
    cat: 'F',
    maturity: 4,
    desc: 'After reconstruction, supernodes shuffle their columns into N chunks and publish chunk by chunk with a delay between them (e.g. 200ms between 4 chunks). Later chunks are often already propagated by other supernodes, avoiding duplicate bandwidth.',
    benefits: ['P2', 'P4'],
    hurts: [],
    openQs: [
      'Interaction with attestation deadlines?',
      'Optimal publication schedule?',
    ],
    refs: ['Sigma Prime blog (Sep 2024)'],
  },
  {
    id: 'F4',
    name: 'Distributed blob building',
    cat: 'F',
    maturity: 4,
    desc: "Multiple supernodes retrieve blobs from the EL, compute KZG cell proofs, and publish data columns, distributing the proposer's computation and bandwidth burden.",
    benefits: ['P11', 'P3', 'P7'],
    hurts: ['P10'],
    openQs: [
      'Race condition: reconstruction and proof computation can happen simultaneously, competing for CPU.',
      'How many supernodes are needed for this to work reliably?',
    ],
    refs: ['Sigma Prime blog: PeerDAS and distributed blob building (Sep 2024)'],
  },
  {
    id: 'F5',
    name: 'Block header-first propagation',
    cat: 'F',
    maturity: 2,
    desc: 'Gossip block headers (with KZG commitments) on a separate fast topic before the full block. Lets supernodes and other nodes start proof computation, blob fetching, and cell validation earlier.',
    benefits: ['P4', 'P11'],
    hurts: [],
    openQs: [
      'New gossip topic means new infrastructure. Worth the complexity?',
      'How do you prevent header-only spam (headers without corresponding data)?',
    ],
    refs: ['Sigma Prime blog (Sep 2024) referencing Dankrad\'s proposal'],
  },

  // ── Category G: Mempool techniques ────────────────────────────────
  {
    id: 'G1',
    name: 'EL mempool pre-seeding',
    cat: 'G',
    maturity: 5,
    desc: "Blobs arrive at the EL via devp2p announce/fetch gossip before the block is proposed. This is \"free\" bandwidth from the builder's perspective. The CL can then use getBlobs to leverage this pre-existing data. This is how blob transactions work on mainnet today.",
    benefits: ['P11', 'P4'],
    hurts: [],
    openQs: [
      "~11 KiB/s per blob per node. At 128 blobs, that's ~1.4 MiB/s. Sustainable for home stakers?",
      'Private blobs bypass the public mempool entirely. As private blob share increases, pre-seeding effectiveness decreases.',
    ],
    refs: ['Is data available in the EL mempool? (cskiraly)', 'EIP-4844', 'Sigma Prime blog'],
  },
  {
    id: 'G2',
    name: 'Sharded blob mempool',
    cat: 'G',
    maturity: 1,
    desc: 'EL nodes subscribe to a subset of mempool shards rather than the full blob pool. Reduces per-node EL bandwidth at the cost of no single node having all blobs.',
    benefits: ['P2', 'P1'],
    hurts: ['P10', 'P14'],
    openQs: [
      'Nonce gaps: EIP-8077 proposes adding sender address and nonce to tx announcements. Simulation results show up to ~36% of blob txs affected in worst-case.',
      'Block builders need access to blobs across all shards. Does this recreate the supernode problem at the EL layer?',
    ],
    refs: ['A new design for DAS and sharded blob mempools (Jun 2025)', 'EIP-8077 nonce gap simulation (Dec 2025)'],
  },
  {
    id: 'G3',
    name: 'EC-aware mempool',
    cat: 'G',
    maturity: 1,
    desc: 'The mempool natively understands erasure coding, enabling EC-based sampling and reconstruction at the EL layer itself. Blurs the EL/CL boundary for data availability. Briefly mentioned in FullDASv2 as a future possibility.',
    benefits: ['P12', 'P1'],
    hurts: ['P10'],
    openQs: [
      'Radical change to the EL. What are the implications for EL/CL separation?',
      'Does this require consensus changes or can it be purely at the networking layer?',
    ],
    refs: ['FullDASv2 (cskiraly, May 2025)'],
  },
  {
    id: 'G4',
    name: 'Sparse blobpool (EIP-8070)',
    cat: 'G',
    maturity: 2,
    desc: 'Introduces custody-aligned probabilistic sampling at the EL blobpool layer, replacing full replication of blob data. For each new type 3 (blob-carrying) transaction, a node fetches the full blob payload with probability p=0.15 ("provider role") and otherwise samples only its CL-aligned custody columns ("sampler role"). Samplers fetch as few as 8/128 cells per blob, yielding an average bandwidth reduction of ~4x. Introduces devp2p eth/71 with cell_mask signaling, GetCells/Cells messages, and Engine API extensions (blobCustodyUpdatedV1, getBlobsV4).',
    benefits: ['P2', 'P4', 'P9', 'P10', 'P14'],
    hurts: [],
    openQs: [
      'cell_mask in NewPooledTransactionHashes applies uniformly to all type 3 txs in the message. Practical at high tx rates?',
      'Provider/sampler decision predictability: attack surface if adversary can predict decisions?',
      'Supernode fingerprinting via larger peerset and request patterns?',
      'Builder inclusion policies for sampled-only blobs: failure rate under adversarial conditions?',
      'RBF impact: does replacement inherit the provider/sampler decision?',
    ],
    refs: ['EIP-8070: Sparse Blobpool (Oct 2025)', 'EthMagicians discussion (eip-8070)'],
  },

  // ── Category H: Security / sampling ───────────────────────────────
  {
    id: 'H1',
    name: 'Subnet-based sampling',
    cat: 'H',
    maturity: 5,
    desc: 'Nodes get their samples by subscribing to column subnets. Sample = column. Queries are linkable (subnet membership is visible). Primary sampling mechanism in PeerDAS.',
    benefits: ['P10', 'P2'],
    hurts: ['P8'],
    openQs: [
      'Linkability: an adversary can observe which subnets a node joins and selectively withhold data for only those subnets, convincing that node of availability while withholding the rest.',
      'SubnetDAS security analysis bounds the fraction of foolable nodes at 5-10% for 2000-10000 nodes.',
    ],
    refs: ['EIP-7594', 'SubnetDAS (Oct 2023)'],
  },
  {
    id: 'H2',
    name: 'Peer-based sampling (req/resp)',
    cat: 'H',
    maturity: 3,
    desc: 'Request specific cells from random peers via req/resp protocol, not via subnet membership. Partially mitigates query linkability because queries go to individual peers rather than being broadcast via subnet joins.',
    benefits: ['P8', 'P13'],
    hurts: ['P10'],
    openQs: [
      'If used as the primary sampling mechanism, it creates liveness concerns. SubnetDAS analysis suggests using peer sampling only for the confirmation rule, not fork choice.',
    ],
    refs: ['EIP-7594', 'PeerDAS original post (Sep 2023)'],
  },
  {
    id: 'H3',
    name: 'LossyDAS (adaptive sampling)',
    cat: 'H',
    maturity: 2,
    desc: 'Tolerate some missed samples and adaptively increase sampling effort. Instead of requiring all k samples to succeed, allow a few failures and adjust the security model accordingly.',
    benefits: ['P4', 'P13'],
    hurts: [],
    openQs: [
      'fradamt\'s analysis shows that tolerating multiple misses requires significantly more total samples. With t tolerated misses, the failure probability blows up. Is the bandwidth tradeoff worth it?',
    ],
    refs: ['LossyDAS (cskiraly et al., Mar 2024)'],
  },
  {
    id: 'H4',
    name: 'Local randomness for sampling',
    cat: 'H',
    maturity: 2,
    desc: "Derive sample indices from local entropy (e.g. VRF output) so an adversary can't predict which cells a node will request next.",
    benefits: ['P13', 'P8'],
    hurts: [],
    openQs: [
      'RANDAO manipulable by proposer?',
      'VRF setup cost?',
    ],
    refs: ['FullDAS (cskiraly, May 2024)'],
  },
  {
    id: 'H5',
    name: 'Confirmation rule layering',
    cat: 'H',
    maturity: 2,
    desc: 'Use subnet-based DAS for fork choice (weaker but liveness-safe) and peer-based DAS only for transaction confirmation (stronger but liveness-sensitive). A liveness failure of the peer sampling layer does not affect consensus.',
    benefits: ['P7', 'P8'],
    hurts: ['P10'],
    openQs: [
      'What is the latency overhead of the confirmation rule?',
      'Can the confirmation rule be optional per-node? fradamt suggests yes.',
    ],
    refs: ['SubnetDAS (Oct 2023)', 'DAS fork-choice (fradamt et al., May 2024)'],
  },

  // ── Category I: Propagation scheduling ────────────────────────────
  {
    id: 'I1',
    name: 'Blob tickets (CL propagation rights)',
    cat: 'I',
    maturity: 1,
    desc: 'A ticket system grants the right to propagate a blob through the CL sampling infrastructure. Tickets are acquired by interacting with an on-chain ticket contract. Each ticket grants: (a) propagate one blob on the CL, and (b) propagate multiple blob txs (up to 16) on the EL mempool. The ticket purchase is where the scarce bandwidth resource is paid for. Tickets can be purchased for future slots (forward tickets), creating a futures market for blob space.',
    benefits: ['P4', 'P2', 'P7', 'P12'],
    hurts: ['P10'],
    openQs: [
      'Forward ticket purchasing (slot N+k) creates a futures market for blob space with potential for speculation and hoarding.',
      'How does the ticket market interact with MEV? Builders can front-run demand or buy tickets to control propagation priority.',
      'Tying blob tx propagation to tickets loosens mempool rules. New spam vectors?',
    ],
    refs: [
      'Scaling the DA layer with blob streaming (QED, fradamt, Julian, Feb 2026)',
      'Blob mempool tickets',
      'On the future of the blob mempool',
      'Variants of mempool tickets',
    ],
  },
  {
    id: 'I2',
    name: 'DA contract (availability recording)',
    cat: 'I',
    maturity: 1,
    desc: 'A system contract that records which blobs are available, queryable both within the EVM and by nodes. At the start of each block, a system call records the versioned hashes into the contract. Implemented as a ring buffer over a recent window (~128 blocks). Beyond the window, users prove inclusion against a versioned_hashes_root in the block header. Once recorded, a blob tx referencing those versioned hashes becomes equivalent to a regular transaction for mempool and inclusion purposes.',
    benefits: ['P7', 'P12', 'P9'],
    hurts: ['P10'],
    openQs: [
      'Storage growth: the system call writing 128 versioned hashes per block has a gas cost. Acceptable overhead?',
      'Reorg handling: if block N records availability but is reorged out, blob txs in block N+1 relying on that recording fail. Not addressed.',
      'The 128-block window covers typical rollup needs, but some L2 challenge periods are longer.',
      'Since the DA contract is queryable within the EVM, regular txs can condition logic on blob availability. New use cases and attack surfaces?',
    ],
    refs: ['Scaling the DA layer with blob streaming (Feb 2026)'],
  },
  {
    id: 'I3',
    name: 'JIT/AOT payload split',
    cat: 'I',
    maturity: 1,
    desc: 'The block payload contains two lists of versioned hashes: jit_versioned_hashes (blobs the builder commits to just-in-time, spot-priced via blob basefee) and aot_versioned_hashes (blobs pre-propagated via tickets, already paid for at ticket purchase). Capacity governed by B_1 (JIT max), B_2 (total max), and R (reserved JIT). AOT pricing uses an EIP-1559-style controller.',
    benefits: ['P1', 'P4', 'P11'],
    hurts: ['P10'],
    openQs: [
      'The R parameter (reserved JIT capacity) is the most sensitive design choice. Too low risks underserving L1 JIT needs; too high forces AOT users through builders.',
      'JIT blobs correspond to today\'s private blobs. Does this entrench builder centralization?',
      'Capacity rollover under sustained overdemand creates complex dynamics with the EIP-1559 controller.',
    ],
    refs: ['Scaling the DA layer with blob streaming (Feb 2026)'],
  },
  {
    id: 'I4',
    name: 'PTC blob inclusion enforcement',
    cat: 'I',
    maturity: 1,
    desc: 'PTC (Payload Timeliness Committee) members observe which blobs have been propagated by a deadline, sample them, and vote on availability. A majority vote determines which versioned hashes the proposer must include in the payload. Provides end-to-end censorship resistance for blob txs via PTC (availability recording) + FOCIL (blob tx inclusion). JIT blobs cannot benefit from PTC guarantees.',
    benefits: ['P13', 'P7'],
    hurts: ['P10'],
    openQs: [
      'PTC committee size and selection mechanism are unspecified. Minimum committee size for security?',
      'The safety override (attesters do not enforce PTC if they locally do not see availability) creates an attack surface.',
      'JIT blobs have strictly weaker CR than AOT. Acceptable for L1-as-rollup?',
    ],
    refs: ['Scaling the DA layer with blob streaming (Feb 2026)', 'EIP-7805: FOCIL'],
  },
];
